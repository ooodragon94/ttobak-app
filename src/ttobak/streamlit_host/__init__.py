"""스트림릿 위에서 또박을 띄우는 다리.

또박 서버(FastAPI 앱)와 화면(HTML/CSS/JS)은 **그대로** 쓴다. 이 패키지는 둘을
스트림릿으로 잇기만 한다.

- 화면은 스트림릿 컴포넌트(v2)로 페이지에 붙는다 (``component.js``).
- 화면이 보내던 HTTP 요청은 컴포넌트 신호로 파이썬에 오고, 파이썬은 그것을
  같은 프로세스 안의 FastAPI 앱에 그대로 전한다 (``host.py``).

그래서 게임 규칙·저장·검증·요청 제한이 집 PC 판과 한 코드다. 한쪽을 고치면
다른 쪽도 같이 고쳐진다.
"""
