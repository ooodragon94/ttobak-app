# 스트림릿 설정. 또박 화면이 페이지 전체를 쓰므로 스트림릿 자체의 장식은 끈다.

[client]
# 오른쪽 위 메뉴(다시 실행, 설정 등)를 최소로. 친구들에게는 필요 없다.
toolbarMode = "minimal"
showErrorDetails = false

[browser]
gatherUsageStats = false

[server]
headless = true
